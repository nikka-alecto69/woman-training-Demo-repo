window.NVXUN_CONFIG = {
  // 本机演示 / GitHub Pages 纯前端模式时留空：用户可直接打开网页使用，数据只保存在浏览器本机。
  // 如需让授权数据进入 /#/admin 后台汇总，请先部署 Cloudflare Worker + D1，
  // 然后把这里改成 Worker 地址，例如：
  // API_BASE: "https://your-worker.workers.dev"
  API_BASE: ""
};
